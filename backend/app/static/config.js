/**
 * GitHub Pages (or any static host) + API on another origin:
 * set your Azure API URL here (no trailing slash). Leave empty when the UI is served by the same FastAPI app.
 * Example: window.__API_BASE__ = 'https://your-app.azurewebsites.net';
 */
window.__API_BASE__ = window.__API_BASE__ || '';
